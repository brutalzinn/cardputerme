# Optional overrides — cardputerme works with zero config.

# Device columns / lines per page for text wrapping.
WRAP_COLS=20
LINES_PER_CARD=7

# How many scrollback lines the server mirrors.
SCROLLBACK_LINES=200
MAX_CARDS=40

# Beep the Cardputer when a question appears (1=on, 0=off).
NOTIFY=1
