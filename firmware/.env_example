# Unused by the current build (platformio.ini lives in firmware/cardputer/,
# which reads firmware/cardputer/.env instead). Kept for reference only.

WIFI_SSID=YOUR_WIFI_SSID
WIFI_PASS=YOUR_WIFI_PASSWORD
WRAP_COLS=20
